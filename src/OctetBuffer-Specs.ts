import { expect } from 'chai';
import { OctetBuffer } from 'OctetBuffer';

/**
 * Unit tests
 */
describe('OctetBuffer', () => {

    var buffer: Buffer;
    var octetBuffer: OctetBuffer;




    describe('position', () => {

        it('', () => {
          buffer = new Buffer("deadbeef", 'hex');
        });
    });
});
